#服务端口
server:
  port: 6082
  # Tomcat线程池配置优化
  tomcat:
    threads:
      max: 300                    # 最大工作线程数
      min-spare: 40               # 最小空闲线程数
    max-connections: 6000         # 最大连接数
    accept-count: 150             # 等待队列长度
    connection-timeout: 30000     # 连接超时时间(30秒)
    max-http-header-size: 8192    # HTTP头最大大小
# 应用名称
spring:
  profiles:
    active: local
  application:
    name: ${prefix.distinction.name:ticketflow}-user-service
  main:
    allow-bean-definition-overriding: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
    servlet:
      load-on-startup: 1
    jackson:
      time-zone: GMT+8
      date-format: yyyy-MM-dd HH:mm:ss
      generator:
        WRITE_NUMBERS_AS_STRINGS: true
  datasource:
    driver-class-name: org.apache.shardingsphere.driver.ShardingSphereDriver
    url: jdbc:shardingsphere:classpath:shardingsphere-user-${spring.profiles.active}.yaml
  data:  
    redis:
      database: 0
      host: 127.0.0.1
      port: 6379
      timeout: 5000                     # 连接超时时间(5秒)
      jedis:
        pool:
          max-active: 400               # 最大活跃连接数
          max-idle: 80                  # 最大空闲连接数
          min-idle: 15                  # 最小空闲连接数
          max-wait: 3000                # 最大等待时间(3秒)
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        ip: 127.0.0.1
bloom-filter:
  name: user-register-bloom-filter
  expectedInsertions: 1000
  falseProbability: 0.01
aj:
  captcha:
    cache-type: redis
    type: default
    water-mark: "我的水印"
    slip-offset: 5
    aes-status: true
    interference-options: 2
    font-style: 1
    font-size: 25
    history-data-clear-enable: false
    req-frequency-limit-enable: false
    req-get-lock-limit: 5
    req-get-lock-seconds: 360
    req-get-minute-limit: 30
    req-check-minute-limit: 30  
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  global-config:
    db-config:
      logic-delete-field: status
      logic-delete-value: 0
      logic-not-delete-value: 1
  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    local-cache-scope: statement
feign:
  sentinel:
    enabled: false
  httpclient:
    enabled: false
  okhttp:
    enabled: true
  compression:
    request:
      enabled: true
      mime-types: text/xml,application/xml,application/json
      min-request-size: 512
    response:
      enabled: true
management:
  metrics:
    tags:
      application: user-service
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'
  health:
    elasticsearch:
      enabled: false
  prometheus:
    metrics:
      export:
        enabled: true
manage:
  username: ${MANAGE_USERNAME:admin}
  # 开发环境弱口令；生产环境必须通过环境变量 MANAGE_PASSWORD 覆盖（或 jasypt 加密），禁止使用默认值
  password: ${MANAGE_PASSWORD:admin123}
jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}
    algorithm: PBEWithMD5AndDES
always_verify_captcha: 1
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      #生成文档所需的扫包路径，一般为启动类目录
      packages-to-scan: com.ticketflow.controller
#knife4j配置
knife4j:
  #是否启用增强设置
  enable: true
  #开启生产环境屏蔽
  production: ${KNIFE4J_PRODUCTION:false}
  #是否启用登录认证
  basic:
    enable: false
  setting:
    language: zh_cn
    enable-version: true
    enable-swagger-models: true