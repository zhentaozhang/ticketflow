spring:
  cloud:
    nacos:
      discovery:
        enabled: false
  data:
    redis:
      host: localhost
      port: 16379

elasticsearch:
  esSwitch: false

delay:
  order:
    cancel: false

jasypt:
  encryptor:
    password: it-test-password

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
